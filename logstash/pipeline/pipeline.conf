input {
  beats {
    port => 5044
  }
}

filter {
  # 这里可以添加任何你需要的过滤器配置
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "%{[@metadata][beat]}-%{+YYYY.MM.dd}"
  }
}